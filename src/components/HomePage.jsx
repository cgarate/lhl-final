import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const HomePage = () => (
  <Card className="container">
    <CardTitle title="Why Don't We..." subtitle="A date planner for busy folks." />
  </Card>
);

export default HomePage;
