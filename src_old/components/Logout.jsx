import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const Logout = () => (

  <Card className="container">
    <CardTitle title="Good to see you!" subtitle="Be safe!" />
  </Card>
);

export default Logout;
